import { FollowRelationData } from './follow-relation-data';

describe('FollowRelationData', () => {
  it('should create an instance', () => {
    expect(new FollowRelationData()).toBeTruthy();
  });
});
